# bbolt database + lock
*.db
*.db.lock

# Local env files. The deployed one is /etc/bsc/bsc.env on the box, holding only
# BSC_MASTER_SECRET; nothing of the sort belongs in the repo.
*.env

# built binaries
/bin/
/bsc
/dist/

# editor/OS
.DS_Store
